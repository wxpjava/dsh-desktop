# 打包版本说明（Windows 本地 vs Mac 云打包）

> 一句话：**本地 Windows 打包用你的本地代码；Mac 云打包用 GitHub 官方代码。** 这是最容易混淆的点，记牢这张表就行。

---

## 一、两条打包路径对比

| 维度 | 🪟 Windows 本地打包 | 🍎 Mac 云打包（GitHub Actions） |
| --- | --- | --- |
| **怎么触发** | `npm run rebuild` 或双击 `一键打包.bat` | GitHub 仓库 → Actions → Run workflow |
| **在哪台机器跑** | 你这台 Windows | GitHub 的 macOS 云主机 |
| **DSH 代码来源** | **本地** `D:\develop\DeepSeek Harness` | **官方** `github.com/deepseek-ai/deepseek-harness` 的 `master` |
| **带不带你的本地改动**（如 `spawn.ts`） | ✅ **会带** | ❌ **不带**（纯官方最新） |
| **产出物** | `dist\DeepSeek Harness Desktop Setup 版本号.exe` | 下载 `mac-arm64` / `mac-x64` 的 `.dmg` + `.zip` |

---

## 二、当前你的状态

- **Windows 包**：用你本地 checkout（含 `spawn.ts` 等改动）组装 → 带你的实验改动；
- **Mac 包**：用官方 `master` → 纯官方最新，**不含**你的改动。

所以现在两个平台的 DSH **版本不一致**（一个带你的改动、一个官方）。**这是正常现象**，只要你自己清楚即可。

---

## 三、想让哪个平台「带上/去掉」你的改动？

### 想让 Mac 也带上你的改动（情况 B）
需要把改动放到 GitHub 上你能访问的仓库（fork），并让 workflow 指向它：

1. 在 GitHub **Fork** `deepseek-ai/deepseek-harness`（到你的账号）；
2. 本地提交改动并推到 fork：
   ```powershell
   cd D:\develop\DeepSeek Harness
   git add <你的文件>
   git commit -m "your change"
   git remote add fork https://github.com/你的用户名/deepseek-harness.git
   git push fork master
   ```
3. 改 `build-mac.yml` 里 clone 地址为你的 fork；
4. commit + push 这个 workflow 改动，再 Run workflow。

> 之后 `rebuild.cjs` 里 Windows 的默认 checkout 也建议改成指向同一份代码，保持两平台一致。

### 想让 Windows 也用官方（不带本地改动）
把本地 checkout 和官方同步（如果你没有需要保留的本地改动）：
```powershell
cd D:\develop\DeepSeek Harness
git stash && git pull   # 暂存本地改动并拉官方最新
```
或直接 `git checkout -- .` 丢弃未提交改动后在本地 pull。

---

## 四、一句话决策

| 你想干什么 | 该用哪条路 |
| --- | --- |
| 要最新的官方功能，Mac 稳定版 | 直接 Run workflow（情况 A） |
| 想测试自己的 `spawn.ts` 改动 | 本地 `npm run rebuild`（带改动） |
| 想打 Mac 包也带自己的改动 | 走情况 B（fork + 改 workflow） |

---

## 五、两个工具怎么指向同一份 DSH？

- **`rebuild.cjs`**：默认 `checkout = D:\develop\DeepSeek Harness`，可用 `--checkout` 或环境变量 `DSH_DESKTOP_DSH_CHECKOUT` 覆盖；
- **`build-mac.yml`**：clone 地址写死在文件里，改它或（更优雅）改成读仓库变量 `vars.DSH_REPO`，之后在仓库 Settings 填变量即可切换，不用改 yml。
